# TavernHelper 인터페이스: 변수 표시 및 플레이어 수정 허용

MVU에는 편리한 설계가 있습니다: AI 응답이 완료된 후, 자동으로 응답 끝에 특수 텍스트 `<StatusPlaceHolderImpl/>`를 추가합니다. 이 텍스트 자체는 단순한 플레이스홀더(자리 표시자)로, 아무런 기능이 없습니다. 하지만 Tavern 정규식을 사용하여 이를 캡처하고 다음과 같이 활용할 수 있습니다:

* **프롬프트 전용 포맷**을 비워두면, 이 텍스트가 AI에게 전송되지 않습니다—즉, 어떤 토큰도 소비하지 않습니다!
* **표시 전용 포맷**을 우리가 보여주고 싶은 어떤 콘텐츠로든 설정할 수 있습니다: 단순한 한 줄 텍스트가 될 수도 있고, 상호작용 가능한 프론트엔드 인터페이스가 될 수도 있습니다.

## 프롬프트 전용 포맷: 인터페이스 플레이스홀더 전송하지 않기

새로운 로컬 정규식을 추가하고, `[전송안함]인터페이스 플레이스 홀더`라고 명명합니다 (아오조라 리의 정규식 명명 관례에 따라, `[전송안함]`은 AI에게 전송하지 않음을, `[숨김]`은 플레이어에게 표시하지 않음을, 또는 `[기능명]`으로 명명합니다):

=== "정규식 설정 참고"
    ```
    스크립트 이름: [전송안함]인터페이스 플레이스 홀더
    찾기 정규표현식: <StatusPlaceHolderImpl/>
    대체: (비워둠)
    적용 범위:
      - [ ] 사용자 입력
      - [x] AI 출력
    임시:
      - [ ] 표시 전용 포맷
      - [x] 프롬프트 전용 포맷
    ```

=== "이미지 참조"
    <p align="center">
      <img src="https://vwdygpvfycixugqsgwgn.supabase.co/storage/v1/object/public/post-images/1980dc04-3f08-4581-88cb-b949a10d585b/fab38fd8-330e-4924-a55c-b7f5aa98db66.png" width="600">
    </p>

## 표시 전용 포맷: 인터페이스 표시하기

마찬가지로, 새로운 로컬 정규식을 추가하고 `[인터페이스]상태바`라고 명명합니다. 먼저, SillyTavern의 Extensions (확장 기능) $\rightarrow$ Regex (정규식) 메뉴에서 새 스크립트를 만들고 아래와 같이 설정해 주세요.

=== "정규식 설정 참고"
    ```
    스크립트 이름: [인터페이스] 상태창
    찾을 정규식: <StatusPlaceHolderImpl/>
    대체할 내용: 아래 내용 참조
    적용 범위:
      - [ ] 사용자 입력
      - [x] AI 출력
    임시:
      - [x] 표시 전용 포맷
      - [ ] 프롬프트 전용 포맷
    ```

=== "이미지 참조"
    이미지대로 설정하면, <span style="color: #7b68ee;">화면에는 대체한 내용이</span> 표시되지만, AI에게 프롬프트로 전송되지는 않습니다:
    
    <p align="center">
      <img src="https://vwdygpvfycixugqsgwgn.supabase.co/storage/v1/object/public/post-images/1980dc04-3f08-4581-88cb-b949a10d585b/885e5860-6008-478e-af5c-89ee63850ced.png" width="600">
    </p>

### 순수 텍스트 인터페이스

`user>`, `{{user}}` 등 SillyTavern의 기본 매크로와 마찬가지로, {{format_message_variable::변수명}} 매크로 역시 AI에게 프롬프트를 전송할 때뿐만 아니라 **화면에 표시될 때도** 실제 값으로 치환됩니다. 따라서 복잡한 코드 대신, `<StatusPlaceHolderImpl/>`를 아래와 같은 간단한 텍스트로 교체하여 변수 값을 표시할 수도 있습니다.

```
💖 하쿠아의 현재 의존도: {{format_message_variable::stat_data.하쿠아.의존도}}
```

이렇게 설정하면 매번 AI의 답변 아래에 위 문구가 자동으로 나타나며, 변수의 실제 수치가 올바르게 표시됩니다.

<p align="center">
  <img src="https://vwdygpvfycixugqsgwgn.supabase.co/storage/v1/object/public/post-images/1980dc04-3f08-4581-88cb-b949a10d585b/0a900339-704c-4537-ab3c-512deee33a95.png" width="600">
</p>

물론, HTML과 CSS를 사용하여 꾸밀 수도 있습니다:

```html
<style>
/* 여기에 CSS 스타일을 작성하세요 */
.status-bar {
  font-size: 14px;
  color: #ff69b4;
  border: 1px solid #ff69b4;
  padding: 5px;
  border-radius: 8px;
}
</style>
<div class="status-bar">
💖 하쿠아 현재 의존도: {{format_message_variable::stat_data.하쿠아.의존도}}
</div>
```

<p align="center">
  <img src="https://vwdygpvfycixugqsgwgn.supabase.co/storage/v1/object/public/post-images/1980dc04-3f08-4581-88cb-b949a10d585b/aafb8397-c4ff-439b-a3c1-e162b5703629.jpg" width="600">
</p>

!!! warning "조언"
    하지만 솔직히 말씀드리면, 저는 **단순히 변수 값만 보여주기 위해** 상태창을 만드는 건 **비추천**합니다. 
    변수 값이 궁금한 플레이어들은 어차피 알아서 **변수 관리자**를 열어보거든요. 단순히 수치만 보여주는 것보다, 여러분은 **훨씬 더 재미있는 일**들을 할 수 있습니다.

<p align="center">
  <video width="600" controls>
    <source src="../assets/videos/折叠状态栏.mp4" type="video/mp4">
  </video>
</p>

### 프론트엔드 인터페이스

복잡한 코드를 포함하는 프론트엔드 인터페이스는 이 튜토리얼의 범위를 벗어납니다... 하지만 사실 **[실시간 프론트엔드 인터페이스 또는 스크립트 작성](https://stagedog.github.io/%E9%9D%92%E7%A9%BA%E8%8E%89/%E5%B7%A5%E5%85%B7%E7%BB%8F%E9%AA%8C/%E5%AE%9E%E6%97%B6%E7%BC%96%E5%86%99%E5%89%8D%E7%AB%AF%E7%95%8C%E9%9D%A2%E6%88%96%E8%84%9A%E6%9C%AC/)**을 사용하면 손쉽게 만들 수 있습니다.

프론트엔드 인터페이스에서는 단순히 변수 값을 표시하거나 간단한 꾸미기만 할 수 있는 것이 아닙니다—플레이어가 인터페이스와 **상호작용**하게 만들 수 있습니다!

**[캐릭터 카드 예제](https://github.com/StageDog/tavern_helper_template/tree/main/%E7%A4%BA%E4%BE%8B/%E8%A7%92%E8%89%B2%E5%8D%A1%E7%A4%BA%E4%BE%8B)**에서는 이 점을 보여주기 위해, 인터페이스에서 직접 하쿠아의 의존도를 증감할 수 있도록 지원합니다. 동시에, 변수 관리자에서 변수를 수정하면 인터페이스에도 실시간으로 업데이트됩니다:

<p align="center">
  <video width="600" controls>
    <source src="../assets/videos/frontend-interface-demo.mp4" type="video/mp4">
  </video>
</p>

하지만 물론 더 복잡한 것들도 만들 수 있습니다. 예를 들어:

* 플레이어가 인터페이스를 통해 변수를 수정하고, 조종하는 선택받은 자에게 퀘스트를 부여 (동시에, `시스템 로그` 변수를 사용하여 플레이어가 무엇을 했는지 기록함으로써, 스토리상 AI가 플레이어가 새 퀘스트를 부여했다는 것을 알 수 있게 함)
* AI 생성 요청
* 갈게임(Galgame) 스타일 인터페이스
* 직접 슬레이 더 스파이어(Slay the Spire) 같은 게임 제작 (네, 이런 종류의 캐릭터 카드가 많이 있습니다)!

요약하자면, PC에서는 **[실시간 프론트엔드 인터페이스 또는 스크립트 작성](https://stagedog.github.io/%E9%9D%92%E7%A9%BA%E8%8E%89/%E5%B7%A5%E5%85%B7%E7%BB%8F%E9%AA%8C/%E5%AE%9E%E6%97%B6%E7%BC%96%E5%86%99%E5%89%8D%E7%AB%AF%E7%95%8C%E9%9D%A2%E6%88%96%E8%84%9A%E6%9C%AC/)**을 사용하는 것을 권장합니다.

!!! tip "모바일 사용자를 위한 팁"
    물론, 모바일 환경에서 복잡한 프론트엔드 인터페이스를 생성하는 것은 매우 어렵습니다... 변수 구조와 아래의 프론트엔드 인터페이스 템플릿을 AI에게 보내서 제작하도록 시도해 볼 수 있습니다.

    ??? note "모바일용 간이 프론트엔드 인터페이스 템플릿"
        인터페이스 내에서 변수를 수정하는 기능이 필요하다면, 아래 프롬프트 외에 **[이 프롬프트]**도 함께 AI에게 전송해야 합니다.

        인터페이스 내에서 AI에게 월드 인포 생성 및 수정, 프리셋 수정, 음악 재생 등을 요청해야 한다면, 필요에 따라 **[대응하는 타입 정의 파일]**도 AI에게 전송해야 합니다.

        !!! tip "PC 사용자 권장 사항"
            물론 PC를 사용 중이라면, [실시간 프론트엔드 인터페이스 또는 스크립트 작성] 기능을 사용하여 AI가 Tavern 웹페이지를 직접 보면서 상태창을 작성하게 하는 것을 더 추천합니다.

        ```html
        <head>
          <style>
          ${순수 CSS 디자인 스타일}
          </style>
          <script type="module">
            ${
              로직 코드
              example: |-
                function populateCharacterData() {
                  const all_variables = getAllVariables();

                  // 변수 경로에 있는 값을 가져오거나 없으면 'N/A' 표시
                  const property_value = _.get(all_variables, 'stat_data.${변수 경로}', 'N/A');
                  $('#${캐릭터}-${변수 경로}').text(property_value);
                  ...
                }

                function toggleSection($header) {
                  // 순수 JavaScript DOM 조작보다 jQuery 사용을 우선시함
                  const $content = $header.next('.section-content');
                  $content.toggleClass('expanded');

                  const $arrow = $header.find('span:last-child');
                  $arrow.text($content.hasClass('expanded') ? '▼' : '►');
                }

                async function init() {
                  await waitGlobalInitialized('Mvu');
                  populateCharacterData();

                  $('.section-header').on('click', function () {
                    toggleSection($(this));
                  });
                }

                $(errorCatched(init));
            }
          </script>
        </head>
        <body>
          <div class="card-body">
            <div class="section">
              <div class="section-header">
                <span>${캐릭터명} 핵심 상태</span>
                <span>▼</span>
              </div>
              <div class="section-content expanded">
                <div class="property">
                  <div class="property-name">${한국어 변수명}</div>
                  <div class="property-value-container">
                    <span class="property-value" id="${캐릭터}-${변수 경로}">--</span>
                  </div>
                </div>
                <div class="property">
                  <div class="property-name">${...}</div>
                  <div class="property-value-container">
                    <span class="property-value" id="${...}">--</span>
                  </div>
                </div>
                ...
              </div>
            </div>
            <div class="section">
              <div class="section-header">
                <span>월드 상태</span>
                <span>►</span>
              </div>
              <div class="section-content">${...}</div>
            </div>
            ...
          </div>
        </body>
        ```

## 회고

여기까지 우리는 `<StatusPlaceHolderImpl/>` 플레이스홀더를 활용하여 다음과 같은 인터페이스를 만들었습니다:

* 표시에 어떤 토큰도 소비하지 않음
* AI는 업데이트가 필요한 변수만 업데이트하면 되며, 기존 상태바처럼 매번 모든 정보를 출력할 필요가 없음

!!! tip "상상력을 제한하지 마세요"
    하지만 여기 인터페이스 튜토리얼에 의해 상상력이 제한되지 않도록 하세요.
    
    굳이 `<StatusPlaceHolderImpl/>`를 사용하여 인터페이스를 표시할 필요는 없습니다. 다른 어떤 방법으로든 표시할 수 있습니다: 직접 출력 형식을 정의하여 AI가 다른 필요한 정보를 출력하게 하고, Tavern 정규식으로 이를 캡처한 뒤 여러분의 인터페이스로 대체할 수 있습니다.

## 메시지 시작 부분에 인터페이스 표시하기

또한, `<StatusPlaceHolderImpl/>` 플레이스홀더를 사용하더라도, Tavern 정규식을 조정하여 메시지 끝(`<StatusPlaceHolderImpl/>`의 실제 위치)이 아닌 **메시지 시작 부분**에 인터페이스를 표시할 수 있습니다:

!!! example "정규식 설정 결과"
    ```
    스크립트 이름: [인터페이스]상태바
    찾기 정규표현식: /(.*)<StatusPlaceHolderImpl/>/s
    대체:
      여러분의 인터페이스
      $1
    적용 범위:
      - [ ] 사용자 입력
      - [x] AI 출력
    임시:
      - [x] 표시 전용 포맷
      - [ ] 프롬프트 전용 포맷
    ```

이 예시를 참고로, 여기 인터페이스 튜토리얼에 의해 상상력이 제한되지 않도록 하세요.
